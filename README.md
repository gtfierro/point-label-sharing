# Label Sharing API

## Setup

```bash
# install virtualenv:
# sudo apt install python3-venv

python3 -m venv venv
. venv/bin/activate
pip install -r requirements.txt
```

## Starting

```bash
. venv/bin/activate
python app.py
```

